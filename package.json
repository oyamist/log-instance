{
  "name": "log-instance",
  "version": "1.5.0",
  "description": "Instance-decorated logging for browser or NodeJS",
  "author": "Karl Lew",
  "homepage": "https://github.com/oyamist/log-instance",
  "license": "MIT",
  "private": false,
  "scripts": {
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "test": "mocha --inline-diffs",
    "test:test": "mocha -w -R min --inline-diffs -g TEST"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "should": "^13.2.3",
    "winston": "^3.2.1"
  }
}
